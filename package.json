{
  "name": "edulearn-root",
  "private": true,
  "scripts": {
    "test:backend": "cd backend && (npm test || exit 0)",
    "test:frontend": "cd frontend && (npm test || exit 0)",
    "test:all": "npm run test:backend && npm run test:frontend"
  }
}
